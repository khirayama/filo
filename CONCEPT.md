# Filo - RSS Reader and Reading

Filo は、RSS で記事を見つけ、記事を読むことに集中するプロダクトである。役割は `RSSリーダーパート`(見つける / 整理する)と `ウェブ記事リーディングパート`(読む / 聴く)に分かれる。購読管理と記事一覧で見つけやすくし、その先の読む・聴く体験を滑らかにつなぐ。

機能を増やすことより、読む行為を邪魔する要素を減らすことを優先する。画面ごとの主目的は 1 つに絞り、不要なボタン、重複した導線、過剰なモード切り替えを避け、1 つの目的を達成する操作経路はできるだけ少数に保つ。

## 主要機能

### RSSリーダーパート

- RSS/Atom 購読
- サイト URL からの feed discovery
- OPML インポート / エクスポート
- 購読へのタグ付け、並び替え、カスタムタイトル
- 記事一覧、購読別記事一覧、タグ絞り込み
- 記事の既読管理
- リーディングリスト
- ブックマーク
- 一覧表示用タイトル翻訳
- 購読 feed の手動更新と処理ステータス確認
- アカウント削除

### ウェブ記事リーディングパート

- 元記事を開いて読む
- 端末 / ブラウザの読み上げ機能を利用した音声再生
- 音読キューへの追加
- キューによる連続再生
- iOS / Android / Web + Extension 間で共有される読み上げキューと再生位置

## 用語

- フィード: RSS/Atom 本体。複数ユーザーで共有される取得元
- 購読: ユーザーがフィードを購読している状態。ユーザーごとのカスタムタイトル、並び順、タグを持てる
- 記事: フィードで配信される記事。記事実体はフィード配下の共有データとして扱う
- 記事状態: 既読、リーディングリスト、ブックマークなど、ユーザーごとに保持する状態
- 記事補足データ: タイトル翻訳、本文抽出など、記事単位で生成しユーザー間で共有するデータ
- タグ: 購読に紐づけるユーザーごとの分類。複数付与できる
- retained article: unsubscribe 後も、リーディングリストまたはブックマークによりユーザーから参照可能な記事

## 基本方針

- ユーザーが操作する一覧・管理の主語は `subscription` とする。`feed` は共有ソース、`article` は共有記事、`article_user_state` はユーザー固有状態として分離する
- 記事補足データは記事単位で生成し、ユーザーごとに複製しない
- フィード更新、タイトル翻訳、本文抽出はユーザーの明示操作で開始する。cron は失敗ジョブの復旧のみを担い、コンテンツの取得・生成は行わない
- 本文抽出はリーディングパート(音読キュー)のために行い、RSSリーダーパートの一覧・詳細では本文を扱わない
- 閲覧履歴は独立機能ではなく既読履歴として扱う
- feed 同一性は MVP では canonical feed URL を基準とし、publisher 単位の自動統合は行わない
- stale 判定は更新異常の断定ではなく、更新がしばらくないことの通知として扱う

## 翻訳方針

- アプリが提供する翻訳は一覧表示用タイトルのみとする。サーバー側で生成し、記事補足データとして共有保存する。クライアント側では翻訳しない
- 本文翻訳はアプリでは提供せず、各プラットフォーム / ブラウザの翻訳機能に委ねる
- タイトル翻訳は理解の補助であり、一覧 UI の主役にはしない。原文言語がユーザーの `readableLanguages` に含まれる場合、翻訳済みデータがあっても原文表示を優先できる
- 生成失敗時は再試行可能な導線を持つ

## 読み上げ方針

- 読み上げはリーディングパートの中核機能とし、端末 / ブラウザの TTS 機能を利用する
- 価値の中心は、音読キューに積んだ記事を連続再生できることに置く
- 実際の Web ページを開いて読むことを前提にし、publisher への送客を損なわない
- 記事本文は、音読キュー追加時に必要な範囲で取得・解決する
- 翻訳して聴きたい場合は、プラットフォーム / ブラウザの翻訳機能を先に適用し、音読キューにはその時点の可視コンテンツ(翻訳後の状態)を取り込む
- キュー、再生中の記事、言語、再生位置はサーバーでユーザーごとに保持し、端末切替時に再開できる
- 読み上げ開始時は記事を既読化する
- TTS の音声品質、速度、バックグラウンド再生可否は各プラットフォームの制約に従う
- 読む体験を阻害する細かな設定や操作は増やさず、開始・追加・継続に必要な最小限へ寄せる

## MVPの境界

- 初期リリース対象は iOS / Android / Web / Browser Extension と backend / API / operations とする
- MVP 完了条件は、対象機能が iOS / Android の mobile / tablet と、Web の mobile / tablet / desktop + Extension で利用可能であることとする
- `RSS で探す` `記事を開いて読む` `音読キューで続けて聴く` が一貫して成立することを重視する
- unsubscribe 後も retained article は保持し、再参照できる。feed 情報は表示できるが、subscription 依存 UI は前提にしない
- offline / flaky network 対応は既読、リーディングリスト、ブックマークの短期ローカル再送に限定する
- OPML インポートは非同期 job とし、partial success を許容する
- アカウント削除は受付、進行表示、再試行、再ログイン時の復活防止を含める

## プラットフォーム

- iOS / Android: mobile / tablet を対象にし、単独アプリで RSS リーダーとリーディングの体験を完結させる
- Web: mobile / tablet / desktop を対象にする。単体では IAB のような体験を提供しにくいため、Browser Extension と組み合わせる
- Browser Extension: Web 本体を補完し、現在ページの記事解決、読み上げ開始、キュー追加などブラウザ常駐が必要な体験を担当する

UI 最適化は各プラットフォームで吸収してよい。ただし、iOS / Android は単独で、Web は Web + Extension の合算で、機能有無、API 契約、状態遷移、権限境界に差を設けない。実装形態より、読むための機能が過不足なくそろうことを優先する。

## 非目標

- 独自推薦
- ソーシャル機能(コメント、リアクション、共有タイムライン)
- publisher 単位の自動統合
- cron による自動 feed 巡回
- RSSリーダーパートでの本文抽出
- 本文翻訳(各プラットフォーム / ブラウザの翻訳機能に委ねる)
- 説明文プレビュー翻訳
- 要約生成
- 永続的なオフライン閲覧、全文キャッシュ、競合解決 UI
- 読書を中断させる過剰な設定、切り替え、操作導線
- プラットフォームごとに異なる機能セット
